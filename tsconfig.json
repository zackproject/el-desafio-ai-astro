{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "baseUrl": "./src",
    "paths": {
      // Replace the relative route with @ .. and the route
      "@components/*": ["components/*"],
      "@utils/*": ["utils/*"]
    }
  }
}
