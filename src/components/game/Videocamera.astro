---
interface Props {
  useCamera: boolean;
}
const { useCamera } = Astro.props;
if (useCamera) {
  try {
    const stream = await navigator.mediaDevices.getUserMedia({ video: true });
    const videoElement = document.getElementById(
      "videoElement"
    ) as HTMLVideoElement;
    videoElement.srcObject = stream;
  } catch (error) {
    console.error("Error al acceder a la cámara:", error);
  }
}
---
<video id="videoElement" autoplay>etry</video>
<style>
  video {
    border-radius: 10px;
    margin-top: 10px;
    margin-bottom: 10px;
    /*width: 100%; */
    height: 100px;
    border: 1px solid white;
  }
</style>
